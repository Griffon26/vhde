Although the files in this directory can be regenerated using the files in the
grammar directory, they are under version control anyway to avoid a dependency
on the ANTLR Parser Generator.

To build and run the generated parser, only the ANTLR C++ runtime is needed.
